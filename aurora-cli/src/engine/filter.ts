import type { ProjectConfig } from "../types/project.js";

export function shouldGenerateFile(
  file: string,
  config: ProjectConfig
): boolean {
  // Future conditional rules will go here.
  return true;
}